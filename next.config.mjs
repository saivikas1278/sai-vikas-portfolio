/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/sai-vikas-portfolio',
  assetPrefix: '/sai-vikas-portfolio',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
